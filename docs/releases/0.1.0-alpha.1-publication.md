# 0.1.0-alpha.1 Publication Record

## Publication

- Version: `0.1.0-alpha.1`
- Tag: `v0.1.0-alpha.1`
- Publication date: `2026-07-30`
- Source commit: `fed467ec0cd52a936f06751cd922efcc259914a1`
- GitHub release: public prerelease
- Draft: false
- Stable/latest-stable: false
- Release manifest status: `public_alpha_prerelease`

Uploaded assets:

- `frontier_protocol-0.1.0-alpha.1.jar`
- `frontier_protocol-0.1.0-alpha.1.jar.sha256`
- `release-manifest.json`
- `LICENSE`

Immutable public JAR URL:

```text
https://github.com/upiscium/FrontierProtocol/releases/download/v0.1.0-alpha.1/frontier_protocol-0.1.0-alpha.1.jar
```

The Release workflow performed pre-publication release-candidate verification,
then anonymous public-URL checksum and byte-identity verification after
publication. The published tag and assets are immutable: they must not be moved,
recreated, replaced, or mutated. This alpha is intended for modpack and
integration testing, not stable use.

## Packwiz Smoke

The current upstream Linux x86-64 packwiz artifact from workflow run
`28793198419` at commit `dfd8b68a4796c763e25bad50265ea1f1233e24f1`
was used only in a disposable directory outside the repository. It does not
provide a version flag. With a relative disposable home to avoid recording a
local path, the exact output of `packwiz --version` was:

```text
Error: unknown flag: --version
Usage:
  packwiz [command]

Available Commands:
  completion  Generate the autocompletion script for the specified shell
  curseforge  Manage curseforge-based mods
  github      Manage projects released on GitHub
  help        Help about any command
  init        Initialise a packwiz modpack
  list        List all the mods in the modpack
  migrate     Migrate your Minecraft and loader versions to newer versions.
  modrinth    Manage modrinth-based mods
  pin         Pin a file so it does not get updated automatically
  refresh     Refresh the index file
  rehash      Migrate all hashes to a specific format
  remove      Remove an external file from the modpack; equivalent to manually removing the file and running packwiz refresh
  serve       Run a local development server
  settings    Manage pack settings
  unpin       Unpin a file so it receives updates
  update      Update an external file (or all external files) in the modpack
  url         Add external files from a direct download link, for sites that are not directly supported by packwiz
  utils       Utilities for managing packwiz itself

Flags:
      --cache string              The directory where packwiz will cache downloaded mods (default ".cache/packwiz/cache")
      --config string             The config file to use (default ".config/packwiz/.packwiz.toml")
  -h, --help                      help for packwiz
      --meta-folder string        The folder in which new metadata files will be added, defaulting to a folder based on the category (mods, resourcepacks, etc; if the category is unknown the current directory is used)
      --meta-folder-base string   The base folder from which meta-folder will be resolved, defaulting to the current directory (so you can put all mods/etc in a subfolder while still using the default behaviour) (default ".")
      --pack-file string          The modpack metadata file to use (default "pack.toml")
  -y, --yes                       Accept all prompts with the default or "yes" option (non-interactive mode) - may pick unwanted options in search results

Use "packwiz [command] --help" for more information about a command.
```

| Check | Result |
| --- | --- |
| `packwiz url add` exited successfully | PASS |
| Exactly one Frontier Protocol `.pw.toml` was created under `mods/` | PASS |
| Metadata name and filename identify Frontier Protocol | PASS |
| Metadata contains exactly the immutable public JAR URL | PASS |
| `download.hash-format` is `sha256` | PASS |
| `download.hash` exists and is non-empty | PASS |
| Metadata contains no Actions URL, local path, or temporary directory | PASS |
| `packwiz refresh` succeeded and preserved the public URL | PASS |
| Referenced asset is a valid ZIP/JAR | PASS |
| JAR contains `META-INF/neoforge.mods.toml` | PASS |
| JAR contains `frontier_protocol.mixins.json` | PASS |
| JAR contains no nested dependency JAR | PASS |

The disposable modpack, downloaded JAR, generated metadata, cache, executable,
and temporary output were not added to the repository.
